# MODEL_DIFF_REPORT — OLD ZaffaLive APK models  ×  current rebuild

> **Read-only.** Generated by `analysis/build_model_diff.py`. No project file was modified. OLD field names/types are taken verbatim from the decompiled Gson classes in `API_SCHEMA.json` (plus param-derived shapes for Seat & sendGift, which have no Gson class). NEW field names/types are parsed directly from `rebuild/backend/prisma/schema.prisma`. Correspondences are curated per model (not fuzzy-matched), so every mapped row is defensible; unmapped fields are reported as *only-in* lists.

## 0. The one structural fact that drives every diff

The OLD app is a **single JSON-RPC gateway** (`qm1.java`, `@POST("/")`) returning **fat, denormalized DTOs** — one screen = one giant object. `bn0` (user) alone is **98 fields** and folds in wallet, VIP, settings, relationship and room-membership state. The rebuild is a **normalized relational schema** (40 tables, BigInt PKs, real FKs): the same information is split across `User / Profile / Wallet / Setting / UserRelation / RoomMember / Seat / VipHistory / Couple / Medal`. So most "missing in new" fields are **not lost — they moved to another table or to the Flutter view-model**. The report flags which case each is.

### Naming translation (applies everywhere)
| OLD convention | NEW convention |
|---|---|
| `gid` / `rid` / `uid` (snake, string ids) | `id` / `roomId` / `userId` (`BigInt`, real FK) |
| `svga_url` / `svga_type` | `animUrl` / `animType` |
| `image` / `pic_url` / `page_image` | `iconUrl` / `coverUrl` / `avatarUrl` |
| `op` / `mic_mute_op` | `micMuted` (+ `micMutedByAdmin`) |
| `pos` | `position` |
| `merge_num` | `qty` |
| `noble_level` / `uidLevel` | `vipLevel` / `VipLevel.level` |
| `create_time` epoch int | `createdAt` `DateTime` |

## 1. Gift  —  OLD `rx4` (50f)  vs  NEW `Gift`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `continuous_url` | `animUrl` | string | string |  |
| `reback_type` | `luckyConfig` | int | json/array | **yes** |
| `pool_coins` | `luckyConfig` | int | json/array | **yes** |
| `subGifts` | `subGifts` | json/array | json/array |  |
| `svga_type` | `animType` | int | int |  |
| `svga_url` | `animUrl` | string | string |  |
| `gid` | `id` | string | int | **yes** |
| `type` | `category` | string | int | **yes** |
| `id` | `id` | int | int |  |
| `name` | `name` | string | string |  |
| `image` | `iconUrl` | string | string |  |
| `price` | `priceCoins` | int | int |  |
| `kind` | `category` | string | int | **yes** |
| `seq` | `sort` | string | int | **yes** |

**① OLD-only — in `rx4`, no field in NEW `Gift`** (36):  
`rightag`, `hide_window`, `continuous_url2`, `banner_url`, `title`, `desc`, `jump_url`, `send_num`, `bar_label_url`, `continuous_flag`, `days`, `bind_uid`, `nick`, `avatar`, `groupId`, `groupLv`, `groupCurLv`, `groupCurNums`, `groupSelect`, `select_nums`, `bid`, `uid`, `etime`, `total_num`, `num`, `uptime`, `currency`, `disabled`, `new_mark`, `resource_idx`, `resource_url`, `ltag`, `rtag`, `preview_url`, `extra_charm`, `leftag`

**② NEW-only — in NEW `Gift`, no field in OLD** (4):  
`level`, `comboEnabled`, `enabled`, `createdAt`

**Relations added in NEW `Gift`:** `txns`→GiftTransaction[]

## 2. Room  —  OLD `ri3` (16f)  vs  NEW `Room`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `ratio` | `mode` | float | int | **yes** |
| `image` | `coverUrl` | string | string |  |
| `name` | `name` | string | string |  |
| `page_image` | `coverUrl` | string | string |  |
| `rid` | `publicId` | int | string | **yes** |
| `isRoomMode` | `mode` | bool | int | **yes** |

**① OLD-only — in `ri3`, no field in NEW `Room`** (10):  
`url`, `gift_index`, `eventId`, `clientImageRes`, `clientLable`, `enableLuckyBox`, `thirdGame`, `token`, `page_image_new`, `gameId`

**② NEW-only — in NEW `Room`, no field in OLD** (13):  
`id`, `ownerId`, `type`, `countryCode`, `tags`, `status`, `isLocked`, `passwordHash`, `seatCount`, `onlineCount`, `agoraChannel`, `createdAt`, `updatedAt`

**Relations added in NEW `Room`:** `owner`→User, `members`→RoomMember[], `seats`→Seat[]

## 3. Room ranking/meta — OLD `C2445et` (43f) vs NEW `Room`+`Ranking`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `needPasswd` | `isLocked` | string | bool | **yes** |
| `rid` | `publicId` | string | string |  |
| `owner` | `ownerId` | string | int | **yes** |
| `type` | `type` | string | int | **yes** |
| `rname` | `name` | string | string |  |
| `passwd` | `passwordHash` | string | string |  |
| `pic_url` | `coverUrl` | string | string |  |
| `create_time` | `createdAt` | string | datetime | **yes** |
| `update_time` | `updatedAt` | string | datetime | **yes** |
| `status` | `status` | int | int |  |
| `topic` | `tags` | string | json/array | **yes** |
| `usercnt` | `onlineCount` | string | int | **yes** |
| `topic_name` | `tags` | string | json/array | **yes** |
| `mic_mode` | `mode` | int | int |  |
| `nationalFlag` | `countryCode` | string | string |  |
| `countryName` | `countryCode` | string | string |  |

**① OLD-only — in `C2445et`, no field in NEW `Room`** (27):  
`label`, `pretty_uid`, `orderNum`, `avatar`, `week_level`, `game_id`, `talk_topic`, `official_type`, `popularity`, `system`, `dayRank`, `weekRank`, `monthRank`, `little_game_type`, `themeName`, `themeUrl`, `room_level`, `game_type`, `role`, `total`, `intro`, `bulletin`, `area`, `load_time`, `admin_limit`, `rname_cn`, `collect`

**② NEW-only — in NEW `Room`, no field in OLD** (3):  
`id`, `seatCount`, `agoraChannel`

**Relations added in NEW `Room`:** `owner`→User, `members`→RoomMember[], `seats`→Seat[]

## 4. Seat  —  OLD sendGift/mic params + `C5619a` seat fields  vs  NEW `Seat`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `pos` | `position` | int | int |  |
| `uid` | `userId` | string | int | **yes** |
| `locked` | `state` | bool | int | **yes** |
| `op` | `micMuted` | int | bool | **yes** |
| `mic_mute_op` | `micMuted` | int | bool | **yes** |
| `mic_mute_op_admin` | `micMutedByAdmin` | int | bool | **yes** |

**① OLD-only — in `joinMic/mute params + C5619a`, no field in NEW `Seat`** (1):  
`clientMicType`

**② NEW-only — in NEW `Seat`, no field in OLD** (3):  
`roomId`, `charmCounter`, `updatedAt`

**Relations added in NEW `Seat`:** `room`→Room

## 5. Room membership/roles — OLD `bn0.role`/`auth_list`  vs  NEW `RoomMember`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `role` | `role` | int | int |  |
| `uid` | `userId` | string | int | **yes** |
| `rid` | `roomId` | string | int | **yes** |

**① OLD-only — in `bn0/t43 role fields`, no field in NEW `RoomMember`** (1):  
`auth_list`

**② NEW-only — in NEW `RoomMember`, no field in OLD** (1):  
`joinedAt`

**Relations added in NEW `RoomMember`:** `room`→Room

## 6. Wallet  —  OLD had NO wallet table (balance on `bn0`)  vs  NEW `Wallet`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `beans` | `beans` | int | int |  |
| `wealthExp` | `coinsTotal` | int | int |  |

**① OLD-only — in `bn0 balance fields (NOT b14 — b14 is an event card)`, no field in NEW `Wallet`** (3):  
`voice2UNeedCoins`, `im2UNeedCoins`, `wealthLv`

**② NEW-only — in NEW `Wallet`, no field in OLD** (6):  
`userId`, `coins`, `vipCoins`, `balance`, `version`, `updatedAt`

**Relations added in NEW `Wallet`:** `user`→User

## 7. GiftTransaction  —  OLD `Action/RoomApi.sendGift` params  vs  NEW `GiftTransaction`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `rid` | `roomId` | string | int | **yes** |
| `uids` | `recipients` | string | json/array | **yes** |
| `gid` | `giftId` | string | int | **yes** |
| `bid` | `giftId` | int | int |  |
| `merge_num` | `qty` | int | int |  |
| `lucky_box` | `luckyPayout` | int | int |  |
| `box_type` | `luckyPayout` | int | int |  |

**① OLD-only — in `sendGift params`, no field in NEW `GiftTransaction`** (3):  
`source`, `draw_ratio`, `draw_xy`

**② NEW-only — in NEW `GiftTransaction`, no field in OLD** (6):  
`id`, `senderId`, `unitPrice`, `totalCoins`, `comboId`, `createdAt`

**Relations added in NEW `GiftTransaction`:** `sender`→User, `gift`→Gift

## 8. VIP / Noble  —  OLD `C5619a` (43f)  vs  NEW `VipLevel`+`VipHistory`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `noble_level` | `level` | int | int |  |
| `vipName` | `name` | string | string |  |
| `vipMedalImg` | `badgeUrl` | string | string |  |
| `level` | `level` | string | int | **yes** |
| `avatarFrame` | `frameUrl` | string | string |  |

**① OLD-only — in `C5619a`, no field in NEW `VipLevel`** (38):  
`actTitles`, `chatBubble`, `auth_list`, `popDialog`, `uidLevel`, `familyTag`, `guildTagUrl`, `guildTag`, `carDiyJson`, `rookieGiftId`, `avatar_small`, `charm_lv`, `best_friend_nick`, `best_friend_avatar`, `avatar`, `time`, `medal`, `charm`, `sex`, `mic_mute_op`, `zone`, `avatar_carton`, `songName`, `mic_mute_op_admin`, `avatarFrameJson`, `carFrame`, `carFrameJson`, `couple_nick`, `couple_lv`, `couple_uid`, `couple_avatar`, `wealthLv`, `wealthFrame`, `isFans`, `clientMicType`, `pos`, `symbol`, `familyLevel`

**② NEW-only — in NEW `VipLevel`, no field in OLD** (5):  
`priceCoins`, `durationDays`, `entryEffectUrl`, `benefits`, `sort`

**Relations added in NEW `VipLevel`:** `history`→VipHistory[]

## 9. User decorations  —  OLD `t43` (35f)  vs  NEW `Profile` + Flutter `SeatDecoration`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `avatar` | `avatarUrl` | string | string |  |
| `avatarFrame` | `avatarFrameUrl` | string | string |  |
| `nick` | `nick` | string | string |  |
| `noble_level` | `vipLevel` | int | int |  |
| `sex` | `gender` | int | int |  |
| `uidLevel` | `vipLevel` | int | int |  |
| `charm_lv` | `charmLevel` | int | int |  |

**① OLD-only — in `t43`, no field in NEW `Profile`** (28):  
`carFrame`, `carFrameJson`, `carDiyJson`, `entryShow`, `rookieGiftId`, `isFans`, `avatar_small`, `best_friend_nick`, `best_friend_avatar`, `actTitles`, `avatarFrameJson`, `chatBubble`, `couple_lv`, `couple_nick`, `couple_uid`, `couple_avatar`, `familyLevel`, `familyTag`, `guildTag`, `guildTagUrl`, `medal`, `role`, `symbol`, `auth_list`, `uid`, `vipMedalImg`, `vipName`, `wealthLv`

**② NEW-only — in NEW `Profile`, no field in OLD** (11):  
`userId`, `birthday`, `signature`, `countryCode`, `language`, `charmExp`, `wealthLevel`, `wealthExp`, `fansCount`, `followingCount`, `updatedAt`

**Relations added in NEW `Profile`:** `user`→User

## 10. Payment  —  OLD `C7355b` (34f)  vs  NEW `Product` + `Order`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `id` | `id` | string | int | **yes** |
| `type` | `kind` | string | int | **yes** |
| `name` | `title` | string | string |  |
| `cost_type` | `kind` | int | int |  |
| `active_days` | `kind` | string | int | **yes** |
| `price` | `priceCents` | string | int | **yes** |
| `url_resource` | `kind` | string | int | **yes** |

**① OLD-only — in `C7355b`, no field in NEW `Product`** (27):  
`lefthour`, `score`, `mall_type`, `select`, `uid`, `gid`, `utime`, `expire_time`, `status`, `count`, `src_type`, `discount_price`, `local_imga_url`, `customize`, `customize_status`, `info`, `used`, `act_url`, `url_front`, `resource_json`, `ownFlag`, `intro`, `head_tips`, `is_flash`, `sweet_value`, `charm`, `leftday`

**② NEW-only — in NEW `Product`, no field in OLD** (6):  
`sku`, `currency`, `coins`, `bonusCoins`, `enabled`, `sort`

**Relations added in NEW `Product`:** `orders`→Order[]

## 11. Agency  —  OLD `fx2` (7f)  vs  NEW `Agency`+`AgencyMember`

**① OLD-only — in `fx2`, no field in NEW `Agency`** (7):  
`self_uinfo`, `target_uinfo`, `days`, `sweet_value`, `hasCp`, `apply_gid_info`, `cp_lv`

**② NEW-only — in NEW `Agency`, no field in OLD** (10):  
`id`, `publicId`, `name`, `ownerId`, `level`, `tag`, `tagUrl`, `badgeUrl`, `memberLimit`, `createdAt`

**Relations added in NEW `Agency`:** `members`→AgencyMember[]

## 12. Ranking  —  OLD `C2445et` rank fields  vs  NEW `Ranking`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `dayRank` | `rank` | int | int |  |
| `weekRank` | `rank` | int | int |  |
| `monthRank` | `rank` | int | int |  |
| `popularity` | `score` | int | int |  |
| `orderNum` | `rank` | int | int |  |

**① OLD-only — in `C2445et rank fields`, no field in NEW `Ranking`** (2):  
`week_level`, `pretty_uid`

**② NEW-only — in NEW `Ranking`, no field in OLD** (6):  
`id`, `board`, `period`, `periodKey`, `subjectId`, `updatedAt`

## 13. User profile  —  OLD `bn0` (98f)  vs  NEW `User` + `Profile`

**Mapped fields (OLD → NEW):**

| OLD field | → NEW field | OLD type | NEW type | ⚠ type change |
|---|---|---|---|:--:|
| `beans` | `— (dropped)` | string | — |  |
| `age` | `birthday` | string | datetime | **yes** |
| `charm` | `charmExp` | string | int | **yes** |
| `level` | `charmLevel` | string | int | **yes** |
| `rid` | `— (dropped)` | string | — |  |
| `symbol` | `signature` | int | string | **yes** |
| `wealthLv` | `wealthLevel` | int | int |  |
| `wealthExp` | `wealthExp` | int | int |  |
| `avatarFrame` | `avatarFrameUrl` | string | string |  |
| `charmLv` | `charmLevel` | int | int |  |
| `chatBubble` | `— (dropped)` | string | — |  |
| `nationalFlag` | `countryCode` | string | string |  |
| `vip_info` | `vipLevel` | string | int | **yes** |
| `supporters_num` | `fansCount` | int | int |  |

**① OLD-only — in `bn0`, no field in NEW `Profile`** (84):  
`listen`, `school`, `modify`, `video_phone`, `isBlack`, `audit_avatar`, `height`, `online`, `singerScore`, `cp_info`, `levelName`, `in_room`, `singerRank`, `isAdventureKing`, `medal`, `medalRes`, `songs`, `perfect`, `cost`, `photos`, `distance`, `days`, `friend`, `lock`, `popular`, `issub`, `tag`, `auth_list`, `nextWealthLvExp`, `voice2UNeedCoins`, `carFrame`, `im2UNeedCoins`, `voice2UNeedDefaultCoins`, `muteOp`, `im2UNeedDefaultCoins`, `muteOpAdmi`, `video2UNeedCoins`, `voice_phone`, `video2UNeedDefaultCoins`, `imReplyWordLimit`, `regtime`, `view_me`, `supporters`, `pretty_uid_info`, `active_level`, `hiddenSettings`, `gameLv`, `roomType`, `banInfo`, `uinfo_extra`, `behaviorBanInfo`, `zone`, `avatarFrameJson`, `isBanned`, `carFrameJson`, `isPresident`, `infoBgImg`, `isAnchor`, `whitelist`, `usong_has_update`, `isEnthusiast`, `uid`, `isRookie`, `mobile`, `real_flag`, `sex`, `clientMicType`, `nick`, `actTitles`, `sign`, `noble_level`, `avatar`, `hasApplyGuild`, `birthday`, `family`, `identity`, `guild_info`, `fansNum`, `gifts`, `focusNum`, `likes`, `pos`, `constellation`, `role`

**② NEW-only — in NEW `Profile`, no field in OLD** (7):  
`userId`, `nick`, `avatarUrl`, `gender`, `language`, `followingCount`, `updatedAt`

**Relations added in NEW `Profile`:** `user`→User

## 14. Critical fields that change behaviour (gifts · rooms · wallet · VIP · seats)

These are the mappings that MUST be correct or the feature misbehaves. Verify each against a live capture before trusting the rebuild's defaults.

**🎁 Gifts**
- gid/id→id
- price→priceCoins
- kind/type→category
- svga_url→animUrl
- svga_type→animType
- subGifts→subGifts (lucky/rocket children)

**🚪 Rooms**
- rid→publicId
- image/page_image→coverUrl
- isRoomMode→mode
- needPasswd→isLocked
- passwd→passwordHash
- usercnt→onlineCount
- mic_mode→mode
- owner→ownerId
- dayRank/weekRank/monthRank→Ranking table

**🎙 Seats/Mic**
- pos→position
- uid→userId
- op/mic_mute_op→micMuted
- mic_mute_op_admin→micMutedByAdmin
- locked→state (0/1/2)
- role→role (0 guest/1 admin/2 owner)
- auth_list (fine-grained perms) has NO new field

**💰 Wallet**
- beans (the only balance field ON bn0) → Wallet.beans
- the COIN balance came from a separate auth-only `wallet.getWalletInfo` endpoint whose Gson model was not decrypted — NEW `coins/vipCoins/balance` have NO confirmed OLD source (mark as needs-confirmation)
- NEW adds `version` for optimistic locking — a rebuild-only concurrency guard

**💸 Send-gift**
- uids→recipients (Json)
- gid/bid→giftId
- merge_num→qty
- lucky_box/box_type→luckyPayout
- draw_ratio/draw_xy (draw-gift stroke) have NO new field
- NEW adds unitPrice/totalCoins/comboId

**👑 VIP/Noble**
- noble_level→VipLevel.level
- vipName→name
- vipMedalImg→badgeUrl
- avatarFrame→frameUrl
- NEW splits per-user grant into VipHistory(startedAt/expiresAt)
- OLD carried worn state (carFrame/chatBubble/couple_*) inline — NEW puts those on Profile/Seat
- avatarFrame→Profile.avatarFrameUrl
- entryShow/chatBubble/carFrame → Flutter SeatDecoration only
- couple_* → NEW `Couple` table
- medal → NEW `Medal`/`UserMedal` tables

**💳 Payments**
- price→Product.priceCents
- name→title
- cost_type→kind
- NEW splits catalog(Product) from purchase(Order); OLD `C7355b` mixed both
- discount_price/sweet_value/charm/expire_time → Order/ledger, not Product

**🏛 Agency**
- OLD `fx2` is a CP/couple-apply payload (self_uinfo/target_uinfo/cp_lv/hasCp), NOT a guild/agency — the NEW Agency has essentially no OLD counterpart; OLD guild data lived in `guildTag`/`guildTagUrl`/`familyTag` on t43/C5619a

**🏆 Ranking**
- day/week/monthRank → single Ranking rows keyed by (board,period,periodKey)
- NEW generalises to any board via `board`/`period` enums; OLD hard-coded day/week/month columns

**👤 User**
- charm→charmExp
- wealthLv→wealthLevel
- supporters_num→fansCount
- vip_info→Profile.vipLevel
- OLD `bn0` fused profile+wallet+settings+relations(98f); NEW splits to User/Profile/Wallet/Setting/UserRelation
- beans/coins moved to Wallet
- hiddenSettings→Setting

## 15. Cross-cutting type & relationship differences

**Type changes (systematic):**
- **All identifiers**: OLD `gid/rid/uid/bid` are **String** (snake_case) → NEW **`BigInt`** surrogate PKs with the old public value preserved as `publicId String @unique`. Any client sending the old string id must resolve it via `publicId`, not the numeric PK.
- **Timestamps**: OLD `create_time/update_time/etime/uptime` are **epoch ints** → NEW **`DateTime`** (`createdAt @default(now())`, `updatedAt @updatedAt`).
- **Booleans**: OLD uses **int 0/1** (`needPasswd`, `isJoin`, `hide_window`, `disabled`) → NEW real **`Boolean`** (`isLocked`, `enabled`, `comboEnabled`).
- **Money/exp**: OLD `beans/coins/wealthExp/popularity` are **int/long** → NEW **`BigInt`** (`charmExp`, `wealthExp`, `coins`, `score`) to avoid 32-bit overflow.
- **Nested blobs**: OLD inlined JSON strings (`carDiyJson`, `avatarFrameJson`, `resource_json`, `subGifts`) → NEW typed **`Json`** columns (`luckyConfig`, `subGifts`) or dedicated tables.

**Relationship changes (denormalized → normalized):**
- OLD `bn0` (1 object) → NEW `User 1—1 Profile`, `User 1—1 Wallet`, `User 1—* UserIdentity`, `User 1—* VipHistory`, `User 1—* Order`, `User 1—* WalletLedger`, `User *—* UserRelation`.
- OLD room `ri3`/`C2445et` (flat) → NEW `Room 1—* Seat`, `Room 1—* RoomMember`, `Room 1—* GiftTransaction`, `Room 1—* RoomMessage`; owner is a real FK `Room.ownerId → User`.
- OLD seat state inlined in `C5619a` (per-seat user DTO) → NEW `Seat` with `@@id([roomId,position])` composite key and FK to `Room`.
- OLD gift `rx4` carried `subGifts` inline → NEW keeps `subGifts Json` but records every send in `GiftTransaction` (FK to Room/User/Gift) — OLD had no persisted transaction model at all (send was fire-and-forget params).
- OLD VIP `C5619a` fused tier definition + per-user grant + worn state → NEW `VipLevel` (definition) + `VipHistory` (grant, startedAt/expiresAt) + `Profile.vipLevel` (cache) + `SeatDecoration` (worn).
- OLD payment `C7355b` fused catalog + purchase → NEW `Product` (catalog) + `Order` (purchase, provider/token) + `WalletLedger` (double-entry).

## 16. Caveats & mis-mappings found while diffing

- **`b14` is not a wallet.** Its fields (`actId, beginTime, endTime, coins, coinsLimit, isJoin, expiredSurplus, themeType, process, limit`) describe a **timed coin-activity / event card**, not a balance. The only balance field on the user object is `bn0.beans`; the coin balance came from a separate auth-only `wallet.getWalletInfo` endpoint whose Gson model was not decrypted. The NEW `Wallet` table is therefore largely a **new construct** — confirm coin vs bean vs vipCoin semantics from a live `wallet.getWalletInfo` capture.
- **`fx2` is not an agency.** It is a **CP / couple apply** payload (`self_uinfo, target_uinfo, cp_lv, hasCp, sweet_value`). It maps to the NEW **`Couple`** table, not `Agency`. The OLD guild/family data lived in `guildTag/guildTagUrl/familyTag/familyLevel` on `t43`/`C5619a`. The NEW `Agency`/`AgencyMember` has no faithful OLD counterpart and needs live confirmation of its intended shape.
- **`draw_ratio` / `draw_xy`** (draw-gift brush stroke coordinates) and **`source`** (send entry-point) from `sendGift` have **no field** in the NEW `GiftTransaction`. If draw-gifts are in scope, add them.
- **`auth_list`** (fine-grained per-user room permissions bitmap) collapses to a single `RoomMember.role` int in NEW — a loss of granularity; verify no admin sub-permission depended on it.
- **Entry effects / chat bubble / car frame** (`entryShow`, `chatBubble`, `carFrame`) exist only in the Flutter `SeatDecoration` view-model, not in any NEW DB table — they must be served from `VipLevel.entryEffectUrl` / medal / decoration lookups at render time.
- Gift `category` (NEW `Int`) fuses TWO old dimensions: `type` and `kind`. Confirm the enum (0 normal/1 vip/2 lucky/3 rocket/4 bomb/5 backpack) against `gift.getGiftList`'s real `type`+`kind` values before trusting routing of lucky/rocket/bomb gifts.

## 17. Coverage

- OLD models compared: **13** source shapes across 10 domains (359 OLD fields examined).
- NEW models parsed from live schema: **40** tables total; **11** directly compared.
- NEW tables with **no OLD counterpart** (rebuild additions): `AdminUser`, `AgencyInvite`, `Announcement`, `AuditLog`, `Ban`, `Banner`, `BottleReaction`, `CommissionRecord`, `DmConversation`, `DmMessage`, `Moment`, `MomentComment`, `MomentLike`, `Notification`, `Report`, `RoomMessage`, `Setting`, `UserIdentity`, `VoiceBottle`, `WalletLedger`, `WithdrawalRequest`.

_End of report. Nothing in the rebuild was modified._