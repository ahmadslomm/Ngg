# MISSING_COMPONENTS.md — الناقص والمفقود (قائمة أدلة)

> القاعدة: أي شيء غير موجود داخل الـ APK = **مفقود**، ويُصنّف حسب إمكانية استرجاعه. لا تخمين.

## أ) مفقود تمامًا من الـ APK (ليس موجودًا أصلًا)

| العنصر | الدليل على الغياب | كيف يُسترجَع |
|---|---|---|
| **الخادم الخلفي (PHP backend)** | التطبيق عميل فقط؛ لا كود خادمي | يُعاد بناؤه من عقد الـ API (263 عملية موثّقة). المالك فقط يملك مصدره الأصلي |
| **مكتبات native `.so`** | `find *.so` = 0؛ manifest `requiredSplitTypes="base__abi"` → في split APKs | Play Console (AAB/universal APK) أو من موزّعي SDK (Agora/Tencent/FaceUnity) |
| **`mapping.txt` (إزالة التعتيم)** | `find mapping.txt` = none؛ كل الأسماء مبهمة/تمويهية | Play Console → Deobfuscation files (الإصدار 150) |
| **مفتاح التوقيع (keystore)** | لا `.jks/.keystore` (الموجود في tools مجرد مفتاح objection تجريبي) | يملكه المالك / Play App Signing |
| **`google-services.json`** | غير موجود كملف | **يُعاد بناؤه** من قيم Firebase المستخرجة (project `waignalo`) |
| **صيغة الـ `sign` والسر** | مبني وقت التشغيل | تتبّع interceptor (`p000/C4089ml`,`wk0`,`r57`) أو Frida |
| **base URL نهائي (host+scheme+path)** | مضيفات مشفّرة (`waignwhalo.com`,`tanmchat.com`)، تُجمّع وقت التشغيل | Frida على `Retrofit.baseUrl()` |
| **أجسام JSON للطلب/الرد** | نماذج Gson مبهمة في `p000/` | Frida لالتقاط الترافيك، أو اشتقاق يدوي |

## ب) مُسترجَع جزئيًا (موجود لكن ناقص المعنى)

| العنصر | الحالة |
|---|---|
| أسماء الأصناف/الدوال | **تمويهية** (R8) — تحتاج `mapping.txt` |
| مخططات Room | الجداول موجودة، أسماء الأعمدة مبهمة |
| نماذج DTO / واجهات Retrofit | ضاعت التعليمات التوضيحية؛ المنطق مفكوك |
| إصدارات Agora/FaceUnity | مُستنتَجة من الحزم؛ Tencent IMSDK = 9.0.7657 (مؤكّد) |

## ج) مُسترجَع بالكامل (لا ينقصه شيء)

- كل الموارد: 110 لغة، 1631 صورة، 639 vector، 718 تخطيط، 749 نمط، 310 لون، 78 أنيميشن.
- كل الأصول: 68 PAG + 85 SVGA + mp4 + حُزم FaceUnity (`ai_face/human/hand_processor.bundle`, `face_beautification.bundle`, `controller_cpp.bundle`، 74MB).
- `AndroidManifest.xml` كامل + المكوّنات + الصلاحيات.
- **طبقة تشفير السلاسل: معكوسة 100%** → 2,672 سلسلة مفكوكة، 263 endpoint، إعدادات الخدمات.
- كل المنطق المفكوك (10,985 Java + 22,769 Smali) كمرجع.
- إعدادات Firebase.

## د) خطوة Frida (جاهزة للتنفيذ عند توفّر جهاز)

هدف السكربت: التقاط ما يتعذّر ساكنًا — الـ base URL، أجسام JSON، وصيغة الـ sign.
نقاط الاعتراض المحدّدة بالدليل:
- `Retrofit.Builder.baseUrl()` و `OkHttpClient` / `Interceptor.intercept()`.
- دالة فك التشفير `p000.jp1.m25813e` (طباعة كل سلسلة تُفَك وقت التشغيل — يكشف base URL المُجمّع).
- interceptor الترويسات (`p000.C4089ml` / `p000.wk0` / `p000.r57`) لكشف صيغة `sign`.
الأدوات مثبّتة في `tools/` (Frida + Objection). **[غير قابل للتنفيذ في بيئة CLI الحالية — بلا جهاز/محاكي.]**

---

## نسبة اكتمال النظام (تقدير قائم على الأدلة)

| المحور | النسبة | ملاحظة |
|---|---:|---|
| الموارد والأصول | **100%** | جاهزة للنقل المباشر |
| البنية المعمارية (خريطة الطبقات) | **90%** | الطبقات محدّدة؛ الربط الدقيق شاشة↔منطق محدود بالتمويه |
| عقد الـ API (الأسماء والمسارات) | **80%** | 263 endpoint + PHP/H5؛ ناقص أجسام JSON والـ sign |
| طبقة التشفير | **100%** | معكوسة بالكامل، فكّ ساكن يعمل |
| منطق التطبيق (كمرجع مقروء) | **60%** | مفكوك كامل لكن بأسماء تمويهية |
| أسماء الكود الأصلية | **0%** | تحتاج `mapping.txt` |
| كود Kotlin/Compose المصدري | **0%** | لا يُعكَس (jadx يعطي Java مكافئًا) |
| المكتبات native | **0%** | في split APKs غير موجودة |
| الخادم الخلفي | **0%** (عميل فقط) | يُبنى من عقد الـ API |
| مشروع قابل للبناء (Debug APK شغّال) | **~15%** | هيكل + موارد تُجمّع؛ المنطق المُعتّم لا يُعاد تجميعه — انظر أدناه |

### لماذا الـ Debug APK الشغّال غير قابل للتحقيق من هذا المدخل
الكود مفكوك من R8-obfuscated Java: أسماء متضاربة، بنى Kotlin/Compose مفقودة، لا `.so`، endpoints تُبنى ديناميكيًا. jadx ينتج **Java مكافئًا وظيفيًا للقراءة**، لا مصدرًا يُعاد تجميعه. الواقعي: مشروع Android Studio يجمّع **الموارد + الهيكل + الـ manifest**، وطبقة المنطق تُعاد كتابتها في Kotlin مستخدمةً الكود المفكوك كمواصفات + عقد الـ API الموثّق. تقدير الجهد لإعادة الكتابة: كبير (تطبيق تجاري كامل).

**نسبة الاكتمال الإجمالية للاستعادة (كأصول + مواصفات، لا كـ APK يُبنى):** ≈ **72%**.
