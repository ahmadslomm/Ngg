<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- DEBUG/STAGING ONLY: allow cleartext HTTP so the app can reach the plain-HTTP
         staging backend (http://<ip>:8099/v1 + ws://<ip>:8099). The RELEASE build keeps
         usesCleartextTraffic="false" from the main manifest — this override is scoped to
         the debug variant via the manifest merger and never ships in a release APK. -->
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />
</manifest>
