# MASTER H5 MAP

> Generated by `gen_reports.py` from `master-dataset.json`. Every figure is a count over
> extracted evidence; nothing here is an estimate. Items without evidence appear as UNKNOWN
> and are never invented.

| Metric | Value |
|---|---:|
| Candidates probed | 111 |
| **Live pages (HTTP 200)** | **32** |
| 404 | 79 |
| Requiring a session (401/403) | 0 |
| Bundles held locally | 62 |
| Pages with archived assets | 32 |

## Live pages

`anchor`, `announcement`, `announcementFamily`, `coinsMerchant`, `cp`, `cpReward`, `friendCenter`, `giftWall`, `luckyBag`, `luckyBox`, `luckyDraw`, `luckyGift`, `magicBox`, `medalRank`, `my_level`, `noble`, `pay`, `pkRank`, `pkReward`, `pkRule`, `rank`, `report`, `roomGroupRule`, `roomParty`, `roomRule`, `roomScoreRank`, `svip`, `task`, `totalRank`, `vipScoreRank`, `wallet`, `wealth_grade`

## Discovery method accuracy

{"confirmed": 31, "highly_likely": 2, "possible": 78}

Of 78 names guessed from convention, **zero existed**. Of 31 names derived from recovered code,
**all 31 existed**. Page names in this system are not guessable (`wealth_grade`, `my_level`,
`roomGroupRule`) — evidence-derived discovery worked, convention-based guessing did not.

## Authentication

**No page requires a session for its HTML shell.** Authentication happens inside the page's JS via
the token in the query string. Asset collection therefore needs no credentials.

## Archived pages

anchor, announcement, announcementFamily, coinsMerchant, cp, cpReward, friendCenter, giftWall, luckyBag, luckyBox, luckyDraw, luckyGift, magicBox, medalRank, my_level, noble, pay, pkRank, pkReward, pkRule, rank, report, roomGroupRule, roomParty, roomRule, roomScoreRank, svip, task, totalRank, vipScoreRank, wallet, wealth_grade
